A very simple tictactoe "ai" script designed to get a 2d array of the gamefield, the 2d array has to conatain numbers 1-9 or 'X'  & 'O' like this:
1 | X | 3
4 | X | 6
O | 8 | 9
And you  give it a boolean which indentify if the plays as player 1 or player 2.
There are 4 different diffilcultys, the last one can´t be beaten!
you call them like this:

aiEasy(Array[][], bool);
aiMedium(Array[][], bool);
aiHard(Array[][], bool);
aiImpossible(Array[][], bool);

they return a number 1-9 which is the field it picked.  
